class storageData{
    //type being session, sync, or local
    constructor(key, type){
        this.key = key;
        this.type = type;
    }
    
    get(){
        chrome.storage[this.type].get([this.key]);
    }
    set(value){
        chrome.storage[this.type].set({[this.key]: value});
    }
}

let isPaused = new storageData("isPaused", "session");
isPaused.set(true);

let playPauseButton = document.getElementById("playPause");
let resetButton = document.getElementById("reset");
let skipButton = document.getElementById("skip");

resetButton.addEventListener('click', async () => {
    console.log("clicked!!!");
});

// chrome.runtime.onClicked.addListener(() => {
//     chrome.action.setBadgeText({
//       text: "paused",
//     });
//     console.log("installed");
// });



//blue icon = break
//red icon = work
//blue text background =  
